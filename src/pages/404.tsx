import Layout from "../components/Layout";

export default function four0three() {
    return <Layout title = "404 - Not Found" canonical = "/404" description = "Not Found">
        <h1 className = "text-center">That page, if it ever existed, doesn&apos;t appear to exist.</h1>
    </Layout>;
}